A* 알고리즘 구현 계획

우선 시작점과 목적지를 임의로 설정한다.
단, 각 지점은 통행이 가능한 길이어야 한다.

시작점에서 시작하며 큐에 넣는다.

큐 제일 앞에 있는 값에서 8방향을 확인하고 각 지점에서 g값과 h값을 구한다.
그 중 가장 f(g+h)가 작은 곳을 큐에 넣는다.
또한 해당 길을 또 갈 수 있으니 방문했다고 저장한다. 0 -> 2


-----

큰 타일에서 우선 탐색
그리고 작은 타일에서 탐색